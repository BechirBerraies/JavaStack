package com.example.demo;

public class test {
    int[][] iArray = { {4, 10, 25}, {16, 8, 32}, {50, 1, 17} };
        for (int x = 0; x < ; x++)
    {iArray.length
        for (int y = 0; y < iArray[x].length; y++)
        {
            if(iArray[x][y] < 10) break;
            System.out.printf("%d ", iArray[x][y]);
        }
    }
}
